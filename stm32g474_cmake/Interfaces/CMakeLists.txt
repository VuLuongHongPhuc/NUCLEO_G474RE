cmake_minimum_required(VERSION 3.23.1)

file(GLOB_RECURSE INTERFACE_C_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/*.c")

set(INTERFACES_SRC
        ${INTERFACE_C_SOURCES}
        PARENT_SCOPE
)

#target_include_directories(INTERFACES_SRC PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
